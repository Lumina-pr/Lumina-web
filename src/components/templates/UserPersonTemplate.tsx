import React from "react";
import UserPerson from "@/components/organisms/UserPerson";

const DevicePageTemplate: React.FC = () => {
    return (
        <main className="p-6 bg-gray-100 min-h-screen">
        <UserPerson />
        </main>
    );
};

export default DevicePageTemplate;
