import React from "react";
import LoginScreen from "@/components/organisms/LoginCard";

const LoginTemplate = () => {
    return (
        <div>
            <LoginScreen />
        </div>
    );
};

export default LoginTemplate;
